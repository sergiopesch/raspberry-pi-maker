name: Publish to ClawHub

on:
  workflow_dispatch:

permissions:
  actions: read
  contents: read
  id-token: write

jobs:
  publish:
    uses: openclaw/clawhub/.github/workflows/package-publish.yml@v0.23.1
    permissions:
      actions: read
      contents: read
      id-token: write
    with:
      dry_run: false
      owner: sergiopesch
